name: Publish to Nuget

on:
  push:
    tags:
      - 'v*' # Triggers on tag pushes starting with 'v'

jobs:
  build-and-publish:
    strategy:
      matrix:
        framework: ['net10.0']
    runs-on: ubuntu-latest
    steps:
      # Checkout the repository
      - uses: actions/checkout@v2

      # Set up .NET SDK
      - uses: actions/setup-dotnet@v2
        with:
          dotnet-version: '10.0.x'

      # Restore dependencies
      - name: Restore dependencies
        run: dotnet restore src/SIPSorceryMedia.Encoders.csproj

      # Build the project
      - name: Build
        run: dotnet build src/SIPSorceryMedia.Encoders.csproj -c Release --no-restore

      # Pack the NuGet package
      - name: Pack NuGet package
        run: dotnet pack src/SIPSorceryMedia.Encoders.csproj -p:NuspecFile=SIPSorceryMedia.Encoders.nuspec -c Release --no-build

      # Publish to NuGet
      - name: Publish NuGet package
        if: startsWith(github.ref, 'refs/tags/v') # Only run on tag pushes
        env:
          NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
        run: |
          dotnet nuget push ./artifacts/*.nupkg \
            --source https://api.nuget.org/v3/index.json \
            --api-key $NUGET_API_KEY
